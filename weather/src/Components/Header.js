import Navbar from './Navbar';
import Cityscroll from '../Pages/Weather/Cityscroll';

const Header=()=>{
    return (
        <header>
            <Navbar/>
            <Cityscroll/>
        </header>
    );
}

export default Header;